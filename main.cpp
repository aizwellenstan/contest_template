%%writefile solution.cpp
#include <bits/stdc++.h>
using namespace std;

bool f(string& o) {
    return 1;
}

int main() {
    ios::sync_with_stdio(0); cin.tie(0);
    string o; while (f(o)); cout << o;
}
